{
    "name": "ebanisteria-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "build": "cd frontend && npm install && npm run build"
    },
    "engines": {
        "node": "22.x"
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "helmet": "^7.1.0",
        "jsonwebtoken": "^9.0.2",
        "morgan": "^1.10.0",
        "pdfkit": "^0.17.2",
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}